function createFloatingHearts() { setInterval(() => { const heart = document.createElement('div'); heart.className = 'heart-particle'; heart.innerHTML = '❤'; heart.style.left = Math.random() * 100 + '%'; heart.style.animationDuration = (Math.random() * 3 + 5) + 's'; heart.style.fontSize = (Math.random() * 10 + 15) + 'px'; document.body.appendChild(heart); setTimeout(() => heart.remove(), 8000); }, 2000); }
function setupMusic() { const music = document.getElementById('bgMusic'); const musicControl = document.getElementById('musicControl'); if (!music || !musicControl) return; let isPlaying = false; musicControl.addEventListener('click', () => { if (isPlaying) { music.pause(); musicControl.innerHTML = '🎵'; isPlaying = false; localStorage.setItem('musicPlaying', 'false'); } else { music.play(); musicControl.innerHTML = '🔊'; isPlaying = true; localStorage.setItem('musicPlaying', 'true'); localStorage.setItem('musicTime', music.currentTime); } }); const wasPlaying = localStorage.getItem('musicPlaying') === 'true'; const savedTime = parseFloat(localStorage.getItem('musicTime')) || 0; music.currentTime = savedTime; if (wasPlaying) { music.play().then(() => { musicControl.innerHTML = '🔊'; isPlaying = true; }).catch(() => {}); } window.addEventListener('beforeunload', () => { if (isPlaying) { localStorage.setItem('musicTime', music.currentTime); } }); }
function setupKeyboardNav() { document.addEventListener('keydown', (e) => { if (e.key === 'ArrowLeft') { const prevBtn = document.querySelector('.nav-btn.prev'); if (prevBtn) window.location.href = prevBtn.href; } else if (e.key === 'ArrowRight') { const nextBtn = document.querySelector('.nav-btn.next'); if (nextBtn) window.location.href = nextBtn.href; } }); }
document.addEventListener('DOMContentLoaded', () => { createFloatingHearts(); setupMusic(); setupKeyboardNav(); });